function sumLength(strOne, strTwo, strThree) {
    let sum = strOne.length + strTwo.length + strThree.length;
    let avarageLength = Math.floor(sum/3);
    console.log(sum);
    console.log(avarageLength);
}